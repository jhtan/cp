#include <iostream>
#include <cstdio>
#include <vector>
#include <algorithm>

#define TRvi(c, it) \
for (vi::iterator it = (c).begin(); it != (c).end(); it++)

using namespace std;

typedef vector<int> vi;

int main() {
	return 0;
}
